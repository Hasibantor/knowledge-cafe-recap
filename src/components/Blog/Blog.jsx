import React from 'react';
import { PropTypes } from "prop-types";

const Blog = ({blog}) => {
    console.log(blog)
    return (
        <div>
            
        </div>
    );
};
 Blog.prototype={
    blog:PropTypes.object
 }
export default Blog;